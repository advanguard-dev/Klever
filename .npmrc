# Electron's Chromium download plus Rolldown/Lightning CSS native
# bindings can look hung if a fetch stalls. Retry and raise timeouts.
fetch-retries=5
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
fetch-timeout=300000
maxsockets=12
fund=false
progress=true
